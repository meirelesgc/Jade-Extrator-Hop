{
  "description" : "Jade-Extrator",
  "company" : "NPAI",
  "metadataBaseFolder" : "${PROJECT_HOME}/metadata",
  "unitTestsBasePath" : "${PROJECT_HOME}",
  "dataSetsCsvFolder" : "${PROJECT_HOME}/datasets",
  "enforcingExecutionInHome" : true,
  "parentProjectName" : "default",
  "config" : {
    "variables" : [ {
      "name" : "ADMIN_DATABASE_URL",
      "value" : "jdbc:postgresql://db:5432/conectee_admin?user=postgres&password=postgres",
      "description" : ""
    },
    {
      "name" : "DATABASE_URL",
      "value" : "jdbc:postgresql://db:5432/conectee?user=postgres&password=postgres",
      "description" : ""
    }]
  }
}